<div class="main">
	<div class="content">
		<%= form_for(@reminder) do |f| %>
			<div class="field">
				<%= f.label :reminder %><br>
				<%= f.text_area :content %>
			</div>
			<div class="actions">
				<%= f.submit "Create" %>
			</div>
		<% end %>
	</div>
</div>